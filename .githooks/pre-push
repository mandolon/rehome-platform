#!/usr/bin/env bash
if [ "$REHOME_AGENT" = "storm" ] || [ -n "$STORM_AGENT" ]; then
  if [ "$STORM_ALLOW_WRITE" != "1" ]; then
    echo "🔒 pre-push: Storm READ-ONLY. Set STORM_ALLOW_WRITE=1 to push." >&2
    exit 1
  fi
fi
exit 0
