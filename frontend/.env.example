# Rehome Platform Frontend Environment Variables

# API Configuration
# Backend API base URL (without /api suffix)
NEXT_PUBLIC_API_URL=http://localhost:9000/api

# Mock API Configuration
# Set to '1' to use mock API instead of real backend
NEXT_PUBLIC_USE_API_MOCK=0

# Mock API base URL (used when NEXT_PUBLIC_USE_API_MOCK=1)
NEXT_PUBLIC_API_BASE_URL=/api